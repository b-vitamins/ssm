"""CUDA fused kernels (stubs).

This package contains CUDA source files for the GPU backend. Build system
integration is deferred; files here define the entry points and doc-comments
that mirror the Python signatures.
"""
